
  <%= form_for(@prediction) do |f| %>
    <% if @prediction.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@prediction.errors.count, "error") %> prohibited this prediction from being saved:</h2>

        <ul>
        <% @prediction.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= f.label :Race %><br>
      <%= f.collection_select(:race, Race.active, :id, :race_name) %>
    </div>
    <div class="field">
      <%= f.label :First %><br>
      <%= f.collection_select(:first, Driver.all, :id, :driver_name, include_blank: true ) %>
    </div>
    <div class="field">
      <%= f.label :Second %><br>
      <%= f.collection_select(:second, Driver.all, :id, :driver_name, include_blank: true) %>
    </div>
    <div class="field">
      <%= f.label :Third %><br>
      <%= f.collection_select(:third, Driver.all, :id, :driver_name, include_blank: true) %>
    </div>
    <br>
    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>
